version: '3'
services:
  app1:
    build: .
    environment:
      - App_NAME=App1
      - Database_URL=postgresql://postgres:joshua081@host.docker.internal:5432/car-arena
    ports:
      - "3001:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"

  app2:
    build: .
    environment:
      - APP_NAME=App2
      - Database_URL=postgresql://postgres:joshua081@host.docker.internal:5432/car-arena      
    ports:
      - "3002:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
 